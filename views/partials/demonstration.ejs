<section class="bg-black text-white py-16 font-poppins px-4">
    <div class="max-w-4xl mx-auto text-center space-y-4">
        <h2 class="text-2xl md:text-4xl font-extrabold">Veja Nossas Soluções em <span class="text-orange-500">Ação</span></h2>
        <p class="text-gray-400 text-base md:text-lg">Assista a demonstrações de nossas soluções de automação Python resolvendo problemas empresariais reais.</p>
    </div>
    <div class="overflow-hidden relative w-full max-w-4xl mx-auto bg-[#111] rounded-lg mt-10">
        <div id="carousel" class="flex transition-transform duration-500 ease-in-out">
            <div class="min-w-full flex flex-col p-6 text-left space-y-6">
                <div>
                    <h3 class="text-lg font-semibold mb-2">Automação de Processamento de Documentos</h3>
                    <p class="text-gray-400 text-sm">Veja como nossa solução extrai dados de faturas automaticamente.</p>
                </div>
                <div class="flex justify-center">
                    <img src="/generating.gif" alt="Automação de documentos" class="w-full max-w-[300px] sm:max-w-[400px] md:max-w-[600px] h-auto object-contain rounded-lg shadow-md"/>
                </div>
            </div>
            <div class="min-w-full flex flex-col p-6 text-left space-y-6">
                <div>
                    <h3 class="text-lg font-semibold mb-2">Pipeline de Análise de Dados</h3>
                    <p class="text-gray-400 text-sm">Assista ao nosso script Python analisar dados de vendas e gerar relatórios.</p>
                </div>
                <div class="flex justify-center">
                    <img src="/generating.gif" alt="Pipeline de dados" class="w-full max-w-[300px] sm:max-w-[400px] md:max-w-[600px] h-auto object-contain rounded-lg shadow-md"/>
                </div>
            </div>
            <div class="min-w-full flex flex-col p-6 text-left space-y-6">
                <div>
                    <h3 class="text-lg font-semibold mb-2">Web Scraping em Ação</h3>
                    <p class="text-gray-400 text-sm">Demonstração da nossa solução de web scraping coletando dados de mercado.</p>
                </div>
                <div class="flex justify-center">
                    <img src="/generating.gif" alt="Web scraping" class="w-full max-w-[300px] sm:max-w-[400px] md:max-w-[600px] h-auto object-contain rounded-lg shadow-md"/>
                </div>
            </div>
        </div>
        <div class="flex justify-center items-center gap-6 py-6">
            <button id="prev" class="text-orange-500 hover:text-orange-400 text-xl">
                <i class="fas fa-chevron-left"></i>
            </button>
            <div id="indicator" class="text-sm text-gray-400">1 / 3</div>
                <button id="next" class="text-orange-500 hover:text-orange-400 text-xl">
                <i class="fas fa-chevron-right"></i>
            </button>
        </div>
    </div>
</section>

<script>
    const carousel = document.getElementById('carousel');
    const slides = carousel.children;
    const indicator = document.getElementById('indicator');
    const totalSlides = slides.length;
    let currentIndex = 0;

    function updateCarousel() {
        const slideWidth = slides[0].offsetWidth;
        carousel.style.transform = `translateX(-${slideWidth * currentIndex}px)`;
        indicator.textContent = `${currentIndex + 1} / ${totalSlides}`;
    }

    window.addEventListener('resize', updateCarousel);
    document.getElementById('prev').addEventListener('click', () => {
        currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;
        updateCarousel();
    });

    document.getElementById('next').addEventListener('click', () => {
        currentIndex = (currentIndex + 1) % totalSlides;
        updateCarousel();
    });

    updateCarousel();
</script>
